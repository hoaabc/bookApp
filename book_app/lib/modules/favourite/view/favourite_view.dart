import 'package:flutter/material.dart';

class FovouriteScreen extends StatelessWidget {
  const FovouriteScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
